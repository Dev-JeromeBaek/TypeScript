/*
    ## Decorator
        - Class Decorator
        - Method Decorator
        - Property Decorator
        - Parameter Decorator

        - 모든 decorator는 function 임.-> function 의 이름을 호출하면 됨.

    - Decorator 코드 작성 준비
        - step 1. 프로젝트 생성
            - mkdir ts-decorator
            - cd ts-decorator
            - yarn init -y
        - step 2. typescript 설치
            - yarn add typescript -D
        - step 3. tsconfig 설정
            - node_modules\.bin\tsc --init
            - experimentalDecorators 추가
        - step 4. vscode 컴파일 설정
            - ${workspaceRoot}\node_modules\.bin\tsc
            - command + shift + <B>
*/ 
